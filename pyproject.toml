[project]
name = "query-router"
version = "0.2.0"
description = "A professional local search query router"
dependencies = [
    "fastapi",
    "uvicorn",
    "typer",
    "pyyaml",
    "pydantic"
]

[project.scripts]
qr = "queryRouter.cli:app"